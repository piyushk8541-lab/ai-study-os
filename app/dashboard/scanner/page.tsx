"use client";
import {useState} from "react";import Link from "next/link";
export default function Scanner(){const[file,setFile]=useState<File|null>(null);return <main className="min-h-screen bg-slate-950 p-5 text-white"><div className="mx-auto max-w-4xl"><Link href="/dashboard" className="text-sm text-blue-300">← Dashboard</Link><h1 className="mt-8 text-4xl font-bold">📷 AI Scanner</h1><p className="mt-2 text-slate-400">Upload a question image. Vision solving will be connected to the secure AI endpoint next.</p><label className="mt-8 flex min-h-64 cursor-pointer flex-col items-center justify-center rounded-3xl border border-dashed border-white/20 bg-white/[0.03] p-8 text-center hover:bg-white/[0.05]"><span className="text-5xl">📷</span><span className="mt-4 font-semibold">{file?file.name:"Tap to choose an image"}</span><span className="mt-2 text-sm text-slate-500">JPG, PNG or WEBP</span><input type="file" accept="image/*" className="hidden" onChange={e=>setFile(e.target.files?.[0]??null)}/></label><button disabled={!file} className="mt-5 rounded-xl bg-blue-400 px-5 py-3 font-semibold text-slate-950 disabled:opacity-40">Scan & Solve</button></div></main>}
