"use client";
import { useState } from "react";
import Link from "next/link";
export default function Teacher(){const[q,setQ]=useState("");const[a,setA]=useState("");const[loading,setLoading]=useState(false);async function ask(){if(!q.trim())return;setLoading(true);setA("");try{const r=await fetch("/api/teacher/ask",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question:q,language:"hinglish"})});const d=await r.json();setA(d.answer||d.error||"No answer received.")}catch{setA("Unable to connect. Check your API configuration.")}finally{setLoading(false)}}return <main className="min-h-screen bg-slate-950 text-white p-5"><div className="mx-auto max-w-4xl"><Link href="/dashboard" className="text-sm text-blue-300">← Dashboard</Link><h1 className="mt-8 text-4xl font-bold">🎓 AI Teacher</h1><p className="mt-2 text-slate-400">Ask any study doubt. AI will explain it step-by-step in your preferred style.</p><div className="mt-8 rounded-3xl border border-white/10 bg-white/[0.03] p-4"><textarea value={q} onChange={e=>setQ(e.target.value)} rows={7} placeholder="Example: Explain electrostatics for JEE Main in Hinglish with an example..." className="w-full resize-none bg-transparent p-3 text-white outline-none"/><button onClick={ask} disabled={loading} className="rounded-xl bg-blue-400 px-5 py-3 font-semibold text-slate-950 disabled:opacity-50">{loading?"Thinking…":"Ask AI Teacher"}</button></div>{a&&<div className="mt-5 whitespace-pre-wrap rounded-3xl border border-blue-400/20 bg-blue-400/5 p-6 leading-7 text-slate-200">{a}</div>}</div></main>}
