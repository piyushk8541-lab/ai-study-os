'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';

type Data={stats:{attempts:number;averageAccuracy:number;weakTopics:number;revisionCompletion:number};subjects:{subject:string;accuracy:number;topics:number}[];recentAttempts:{accuracy:number;created_at:string}[];weaknesses:{subject:string;topic:string;weakness_score:number;accuracy:number;status:string}[];profile:{name?:string;target_exam?:string}|null};
export default function Progress(){const[d,setD]=useState<Data|null>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/progress',{cache:'no-store'}).then(async r=>{const x=await r.json();if(!r.ok)throw Error(x.error);setD(x)}).catch(x=>setE(x.message||'Unable to load progress'))},[]);if(e)return <main className="min-h-screen bg-slate-950 p-6 text-white">{e}</main>;if(!d)return <main className="min-h-screen bg-slate-950 p-6 text-white">Loading progress…</main>;return <main className="min-h-screen bg-slate-950 p-4 text-white sm:p-6"><div className="mx-auto max-w-6xl"><Link href="/dashboard" className="text-sm text-blue-300">← Dashboard</Link><h1 className="mt-7 text-3xl font-bold sm:text-4xl">📊 Progress & Analytics</h1><p className="mt-2 text-slate-400">Your learning performance, weak areas and revision health in one place.</p><div className="mt-7 grid gap-3 sm:grid-cols-4"><Stat t="Avg. Accuracy" v={`${d.stats.averageAccuracy}%`}/><Stat t="Exam Attempts" v={d.stats.attempts}/><Stat t="Weak Topics" v={d.stats.weakTopics}/><Stat t="Revision Done" v={`${d.stats.revisionCompletion}%`}/></div><section className="mt-7 rounded-3xl border border-white/10 bg-white/[0.03] p-5"><h2 className="text-xl font-bold">📚 Subject Performance</h2><div className="mt-4 space-y-4">{d.subjects.map(s=><div key={s.subject}><div className="mb-2 flex justify-between text-sm"><span>{s.subject}</span><span>{s.accuracy}%</span></div><div className="h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-blue-500" style={{width:`${Math.min(100,Math.max(0,s.accuracy))}%`}}/></div></div>)}{!d.subjects.length&&<p className="text-slate-500">Subject analytics will appear after exam activity.</p>}</div></section><section className="mt-7 grid gap-7 lg:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-white/[0.03] p-5"><h2 className="text-xl font-bold">🎯 Weak Areas</h2><div className="mt-4 space-y-3">{d.weaknesses.filter(x=>x.status!=='resolved').slice(0,10).map((w,i)=><div key={`${w.topic}-${i}`} className="rounded-2xl border border-white/10 p-4"><div className="flex justify-between gap-3"><div><p className="font-semibold">{w.topic}</p><p className="text-xs text-slate-500">{w.subject} · {w.accuracy}% accuracy</p></div><span className="text-sm text-slate-400">{Math.round(w.weakness_score)}/100</span></div></div>)}{!d.weaknesses.filter(x=>x.status!=='resolved').length&&<p className="text-slate-500">No active weaknesses yet.</p>}</div></div><div className="rounded-3xl border border-white/10 bg-white/[0.03] p-5"><h2 className="text-xl font-bold">📈 Recent Accuracy</h2><div className="mt-4 space-y-2">{d.recentAttempts.slice(0,10).map((x,i)=><div key={i} className="flex items-center justify-between rounded-xl bg-white/5 p-3"><span className="text-sm text-slate-400">Attempt {d.recentAttempts.length-i}</span><span className="font-semibold">{Math.round(Number(x.accuracy??0))}%</span></div>)}{!d.recentAttempts.length&&<p className="text-slate-500">Complete an exam to start your performance history.</p>}</div></div></section></div></main>}
function Stat({t,v}:{t:string;v:string|number}){return <div className="rounded-2xl border border-white/10 bg-white/[0.03] p-5"><p className="text-sm text-slate-400">{t}</p><p className="mt-1 text-2xl font-bold">{v}</p></div>}
